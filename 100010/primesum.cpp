#include <cstdlib>
#include <fstream>
#include <cstring>

using namespace std;

ifstream cin("primesum.in");
ofstream cout("primesum.out")

int main(int argc, char *argv[])
{
    int a,b;
    cin>>n;
    if(n%2==0)
        cout<<a;
    n/2=a,b;
    if(a%b!=0)
        cout<<a<<b;
    cout<<a+b<<endl;
    return EXIT_SUCCESS;
}
